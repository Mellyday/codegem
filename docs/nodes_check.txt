---

What we need to confirm work

Import
function definition (params, positional only, keyword only, default values, args & kwargs),
class definition (inheritance, metaclass, decorators),
generator function
decorator in general

Dictionary

DEEP ASSIGNMENT DIVES
assignment, walrus operator, binary operator, comparison operator
deep call chains/attributes, subscripts & slices
lambda functions?
comprehensions of any kind

OTHER STUFF 
nested classes 
nested functions
type parameters
f-strings
Annotations on class vars
global/nonlocal 

pattern matching
exception handling
context manager 

---

One thing I can confirm is that my previous attempt at making a special reveal/mask logic for 
decorator only didn't work


file grouping/slicing
some files are big and should be split into several quiz/lessons

---

ASK TO SUMMARIZE

grouping logic (code)
Ask claude:
What I mean is that I want you to summarize the quiz behaviors based on @pyengine
Like for example what does common node/statements does it gets turned into mcq right? What kind of nodes are those.
And there are special nodes right. Like function definition, or import for example with different question type, 
grouping logic.
I want an exhaustive look at the behavior we want based on our discussion.
reference answer: https://chatgpt.com/c/694d656c-501c-8324-a3e7-2698f0e19bd0

---

Languages:
Add js/ts, golang, java, ruby, kotlin, php, c, c++, rust, swift, kotlin, c-sharp, scala, rust, obj-c